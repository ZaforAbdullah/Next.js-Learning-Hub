/**
 * PostCSS Configuration
 *
 * PostCSS is a tool for transforming CSS with JavaScript plugins.
 * Tailwind CSS requires both tailwindcss and autoprefixer plugins.
 */

module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
